"""检索层（对应学习手册 04 篇）。

bm25.py    : 关键词检索（BM25 + jieba 中文分词）
hybrid.py  : 混合检索（向量 + BM25，RRF 融合）
rerank.py  : 重排（CrossEncoder 本地 / LLM 两种后端对照）
filters.py : 元数据过滤（含多租户强制过滤）

检索是 RAG 在线的第一公里：
  单纯向量检索会漏掉专有名词/精确匹配；单纯关键词检索理解不了语义。
  因此大规模场景标配 = 多路召回 + 重排。
"""
